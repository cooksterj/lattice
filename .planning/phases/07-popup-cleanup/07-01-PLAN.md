---
phase: 07-popup-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lattice/web/static/js/graph.js
  - src/lattice/web/templates/index.html
autonomous: true

must_haves:
  truths:
    - "No window.open calls exist anywhere in the JavaScript codebase"
    - "No popup-blocked notice, refocus button, or named window targeting code exists in the codebase"
    - "Graph page loads, nodes render, click selects, Execute button works, sidebar navigation works, live logs page works"
  artifacts:
    - path: "src/lattice/web/static/js/graph.js"
      provides: "DAG visualization with all popup code removed"
      must_not_contain: ["openAssetWindow", "showPopupBlockedNotice", "assetWindows", "lattice_graph", "window.open", "GRAF-02"]
    - path: "src/lattice/web/templates/index.html"
      provides: "Main page template with updated cache buster"
      contains: "graph.js?v=18"
  key_links:
    - from: "src/lattice/web/static/js/graph.js"
      to: "handleNodeClick"
      via: "node click handler (line ~529)"
      pattern: "handleNodeClick"
    - from: "src/lattice/web/static/js/graph.js"
      to: "selectNodeForExecution"
      via: "selection logic called by handleNodeClick"
      pattern: "selectNodeForExecution"
---

<objective>
Remove all v1 popup infrastructure from the JavaScript codebase: the `openAssetWindow()` method, `showPopupBlockedNotice()` method, `assetWindows` Map, and `window.name = 'lattice_graph'` assignment. Bump the cache buster in `index.html` to ensure browsers fetch the cleaned-up file.

Purpose: Complete the v2.0 migration by eliminating dead code left behind when Phase 6 replaced the popup click handler with click-to-select. This is the final phase of the v2.0 milestone.

Output: Clean `graph.js` with ~68 lines of dead code removed, updated cache buster in `index.html`, passing test suite, and zero grep matches for popup-related identifiers.
</objective>

<execution_context>
@/Users/jonathancook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jonathancook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-popup-cleanup/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove popup dead code from graph.js and bump cache buster</name>
  <files>
    src/lattice/web/static/js/graph.js
    src/lattice/web/templates/index.html
  </files>
  <action>
    Remove exactly 4 dead code artifacts from `graph.js`:

    1. **Constructor lines (~26-28):** Remove the comment `// Window tracking for asset live monitoring (GRAF-02)`, the `this.assetWindows = new Map();` line, and the `window.name = 'lattice_graph';` line. After removal, ensure the constructor flows cleanly from `this.selectedNode` into `this.executionState` with no orphaned blank lines or comments.

    2. **`openAssetWindow(assetId)` method (~lines 632-654):** Remove the entire method definition (23 lines). This method was the v1 click handler entry point for opening popup windows. Phase 6 replaced the click handler with `handleNodeClick()`. Remove any blank line left between the preceding method and the next method.

    3. **`showPopupBlockedNotice(url)` method (~lines 656-696):** Remove the entire method definition (41 lines). This was only called by `openAssetWindow()`. Remove any blank line left between the preceding method and the next method.

    4. **Cache buster in `index.html`:** Change `graph.js?v=17` to `graph.js?v=18` in the script tag.

    **DO NOT TOUCH** any of these (they are actively used):
    - `handleNodeClick(d)`, `selectNodeForExecution(d)`, `deselectNode()`, `updateExecuteButtonLabel()`
    - `startExecution()`, `connectExecutionWebSocket()`
    - `selectNode(node)` (sidebar detail panel)
    - All node highlighting (`highlightConnections`, `clearHighlights`)
    - All execution state management, memory display, sparkline, date selection
  </action>
  <verify>
    Run: `.venv/bin/python -m pytest tests/ -v`
    All tests must pass.
  </verify>
  <done>
    `graph.js` contains no `openAssetWindow`, `showPopupBlockedNotice`, `assetWindows`, `window.name = 'lattice_graph'`, or `GRAF-02` references. `index.html` loads `graph.js?v=18`. Test suite passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify zero popup references across entire source tree</name>
  <files>
    src/lattice/web/static/js/graph.js
    src/lattice/web/templates/index.html
  </files>
  <action>
    Run grep verification against the entire `src/` directory for every popup-related identifier. Each search MUST return zero matches:

    1. `window.open` — no popup calls
    2. `openAssetWindow` — no method or reference
    3. `showPopupBlockedNotice` — no method or reference
    4. `assetWindows` — no property or reference
    5. `popup-blocked-notice` — no CSS class
    6. `popup.blocked` or `popup blocked` (case-insensitive) — no text
    7. `lattice_graph` — no named window targeting
    8. `lattice_asset_` — no window name prefix
    9. `GRAF-02` — no old requirement tag
    10. `refocusMainWindow` — no refocus function
    11. `window.name` (in graph.js only) — no named window assignment
    12. `window.opener` — no opener references

    If ANY grep returns a match in `src/`, the task fails. Fix the match (it means dead code was missed) and re-verify.

    Also verify functional correctness by running the test suite one final time.
  </action>
  <verify>
    All 12 grep searches return 0 matches in `src/`.
    `.venv/bin/python -m pytest tests/ -v` passes.
  </verify>
  <done>
    Zero popup-related identifiers exist anywhere in `src/`. Test suite passes. The codebase is clean of all v1 popup infrastructure.
  </done>
</task>

</tasks>

<verification>
1. **Test suite:** `.venv/bin/python -m pytest tests/ -v` — all tests pass
2. **Grep sweep:** All 12 popup-related terms return zero matches in `src/`
3. **Cache buster:** `index.html` references `graph.js?v=18`
4. **Code integrity:** `graph.js` still contains `handleNodeClick`, `selectNodeForExecution`, `deselectNode`, `updateExecuteButtonLabel`, `startExecution`, `connectExecutionWebSocket`
</verification>

<success_criteria>
- No `window.open` calls exist anywhere in the JavaScript codebase
- No popup-blocked notice, refocus button, or named window targeting code exists
- All existing functionality (graph, monitoring, history, live logs) continues to work (test suite passes)
- Cache buster bumped so browsers fetch the updated graph.js
</success_criteria>

<output>
After completion, create `.planning/phases/07-popup-cleanup/07-01-SUMMARY.md`
</output>
